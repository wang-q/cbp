Display cbp installation directory paths.

Examples:
1. Show home directory:
   `cbp prefix`

2. Show binary directory:
   `cbp prefix bin`

3. Show library directory:
   `cbp prefix lib`

4. Show include directory:
   `cbp prefix include`

5. Show cbp executable path:
   `cbp prefix exe`
